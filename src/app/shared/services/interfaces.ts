// interfaces.ts

// Define the Testimonial interface
export interface Testimoniale {
  id :number;
  name : string;
  tagline: string;
  title: string;
  description : string;
  // You can add other properties here if needed
}

